
HISTORY:

$ GBAK -r WHERE_FILES:atlas.gbak WHERE_GDB:atlas_join_2.gdb
$ CREATE join002.e

/*
 Out join example 2.9.1 page 64 - 2
 
*/


DATABASE atlas = FILENAME "WHERE_GDB:atlas_join_2.gdb";
#include <stdio.h>
#include "WHERE_FILES:sh_test.h"

BASED_ON CITIES.CITY		City;
BASED_ON STATES.STATE_NAME	State_Name;

short StateFlag;


EXEC SQL
	INCLUDE SQLCA;


main()
{

EXEC SQL
	DECLARE C CURSOR FOR
	SELECT C.CITY, S.STATE_NAME  
	FROM CITIES C LEFT JOIN STATES S ON S.STATE = C.STATE
	WHERE C.CITY STARTING WITH 'San';
	
EXEC SQL
	OPEN C;


printf("City \t\t\t State\n");
printf("---- \t\t\t ------\n");

do{
	EXEC SQL FETCH C INTO :City, :State_Name:StateFlag;
	if (!SQLCODE){
		if (StateFlag)
			strcpy(State_Name,"- NULL -");
		printf ("%-20s ",City);
		printf (" %-20s\n", State_Name);
	}
}while (SQLCODE == 0);

EXEC SQL CLOSE C;

printf("... End of example 2.9.1 \n");
}
$ GPRE -e join002.e
$ CC FLAG_CC join002.c
$ LINK FLAG_LINK join002 
$ RUN join002
$ CREATE join002.qli

ready WHERE_GDB:atlas_join_2.gdb;
set semicolon;
SELECT C.CITY, S.STATE_NAME
FROM CITIES C LEFT JOIN STATES S ON S.STATE = C.STATE
WHERE C.CITY STARTING WITH 'San';
finish;
quit;
$ QLI -n -a join002.qli
$ CREATE join002.sql
connect WHERE_GDB:atlas_join_2.gdb;
SELECT C.CITY, S.STATE_NAME
FROM CITIES C LEFT JOIN STATES S ON S.STATE = C.STATE
WHERE C.CITY STARTING WITH 'San';
quit;
$ISQL -m -i join002.sql
$DROP WHERE_GDB:atlas_join_2.gdb
$ DELETE join002* 
